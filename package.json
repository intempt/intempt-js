{
  "name": "intemptjs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --mode production --minify true",
    "build:dev": "tsc && vite build --mode development --minify false"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.8",
    "@types/web": "^0.0.142",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "uuid": "^9.0.1"
  }
}
